#include <bits/stdc++.h>
#define io cin.tie(NULL), ios_base::sync_with_stdio(false)
using namespace std;
using ll = long long;

int main() {
    io;
    int a, b, c, n, m;
    cin >> a >> b >> c;
    cin >> n >> m;
    ll aa = 1e18, bb = 1e18;    
    // aa = min(n, a + c);
    // bb = min(m, b + c);
    if (a+c < n) {
        
    }
    return 0;
}